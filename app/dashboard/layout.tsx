import React from 'react'

const Layout = ({ children } : {children: React.ReactNode }) =>
{
    return (
        <div>
            <h1 className="text-3xl">A Dashboard </h1>
        </div>
    )
}
export default Layout
